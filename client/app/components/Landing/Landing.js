import React, { Component } from 'react';
import 'whatwg-fetch';

class Landing extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>

            </div>
        );
    }
}

export default Landing;
